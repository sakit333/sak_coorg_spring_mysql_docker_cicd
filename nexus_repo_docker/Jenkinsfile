pipeline {
    agent any

    parameters {
        choice(
            name: 'ACTION',
            choices: ['start', 'stop', 'terminate'],
            description: 'Choose what to do with the Nexus Repository container'
        )
    }

    environment {
        COMPOSE_FILE = 'nexus_repo_docker/docker-compose.yml'
        NETWORK_NAME = 'sak-network'
    }

    stages {
        stage('Validate Network') {
            when { expression { params.ACTION == 'start' } }
            steps {
                echo "🔍 Checking if Docker network '${NETWORK_NAME}' exists..."
                sh '''
                if sudo docker network inspect ${NETWORK_NAME} >/dev/null 2>&1; then
                  echo "✅ Network '${NETWORK_NAME}' already exists."
                else
                  echo "⚙️  Network '${NETWORK_NAME}' not found. Creating it now..."
                  sudo docker network create ${NETWORK_NAME}
                fi
                '''
            }
        }

        stage('Build Nexus Image') {
            when { expression { params.ACTION == 'start' } }
            steps {
                echo "🛠️ Building Nexus Repository image..."
                sh "sudo docker-compose -f ${COMPOSE_FILE} build nexus"
            }
        }

        stage('Start Nexus Container') {
            when { expression { params.ACTION == 'start' } }
            steps {
                echo "🚀 Starting Nexus Repository container..."
                sh "sudo docker-compose -f ${COMPOSE_FILE} up -d nexus"
            }
        }
        stage('Get the admin password for Nexus') {
            when { expression { params.ACTION == 'start' } }
            steps {
                script {
                    def containerName = "nexus-repo"
                    echo "🔐 Waiting for Nexus container (${containerName}) to start..."

                    // Wait until container is up
                    timeout(time: 2, unit: 'MINUTES') {
                        waitUntil {
                            def status = sh(
                                script: "sudo docker ps -f 'name=${containerName}' --format '{{.Status}}'",
                                returnStdout: true
                            ).trim()
                            if (status.contains('Up')) {
                                echo "✅ Nexus container is running."
                                return true
                            }
                            echo "⏳ Waiting for Nexus container to start..."
                            sleep 5
                            return false
                        }
                    }

                    // Wait for admin password file creation
                    echo "🔎 Checking for /nexus-data/admin.password..."
                    timeout(time: 3, unit: 'MINUTES') {
                        waitUntil {
                            def fileExists = sh(
                                script: "sudo docker exec ${containerName} sh -c 'test -f /nexus-data/admin.password && echo exists || echo missing'",
                                returnStdout: true
                            ).trim()
                            if (fileExists == "exists") {
                                echo "✅ admin.password file found."
                                return true
                            }
                            echo "⏳ Waiting for Nexus to finish initialization..."
                            sleep 5
                            return false
                        }
                    }

                    // Retrieve the admin password
                    def adminPassword = sh(
                        script: "sudo docker exec -it ${containerName} cat /nexus-data/admin.password",
                        returnStdout: true
                    ).trim()

                    // Display credentials clearly
                    echo "\n================ Nexus Admin Credentials ================"
                    echo "🌐 URL: http://<your-server-ip>:8081"
                    echo "👤 Username: admin"
                    echo "🔑 Password: ${adminPassword}"
                    echo "==========================================================\n"

                    // Pause pipeline so user can copy it
                    input message: "📋 Copy the above password and click 'Proceed' when ready."
                }
            }
        }

        stage('Stop Nexus Container') {
            when { expression { params.ACTION == 'stop' } }
            steps {
                echo "🛑 Stopping Nexus Repository container..."
                sh "sudo docker-compose -f ${COMPOSE_FILE} stop nexus"
            }
        }

        stage('Terminate Nexus Container') {
            when { expression { params.ACTION == 'terminate' } }
            steps {
                echo "❌ Removing Nexus Repository container, image, and volumes..."
                sh '''
                sudo docker-compose -f ${COMPOSE_FILE} down -v || true

                echo "🧹 Cleaning up Nexus images..."
                sudo docker rmi -f sonatype/nexus3 || echo "⚠️ Base image not found, skipping..."
                sudo docker rmi -f nexus_repo_docker_nexus || echo "⚠️ Custom Nexus image not found, skipping..."
                '''
            }
        }

        stage('Show Nexus Status') {
            steps {
                echo "📦 Current Nexus Repository container status:"
                sh "sudo docker-compose -f ${COMPOSE_FILE} ps"
                echo "🌐 Current Docker networks:"
                sh "sudo docker network ls | grep ${NETWORK_NAME} || true"
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline completed successfully for action: '${params.ACTION}'"
        }
        failure {
            echo "❌ Pipeline failed during action: '${params.ACTION}'"
        }
        always {
            echo "ℹ️ Pipeline finished. Check logs above for details."
        }
    }
}
